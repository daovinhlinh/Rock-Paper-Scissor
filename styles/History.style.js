import { StyleSheet, Dimensions } from "react-native";

export default StyleSheet.create({
    container: {
        color: "#fff",
        fontWeight: "bold",
        fontSize: 20,
        margin: 2,
        width: 30,
        height: 30,
        textAlign: "center",
        borderRadius: 5,
    },
});
